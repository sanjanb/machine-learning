## Normalisation is the technique applied as a part of data preparation, the main aim is to use the common scale in the machine learning dataset

### commonly used algorithms is MinMaxScaling, MeanNormalisation, MaxAbsoluteScaling, RobustScaling

formula = Xi = (Xi - Xmin) / (Xmax - Xmin), the output distribution is in between [0, 1]
