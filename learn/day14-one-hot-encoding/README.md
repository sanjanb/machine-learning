a famous technique one hot encoding used on nominal data
once we get the multiple column from the one hot encoding then we have to exclude one column because of mutlicolliniarity
There should be no mathematical relationship between the input features(adding all the dummy columns gives answer 1, therefore there is a relationship, if we delete one column we get independent column)
<br>
if we have more columns eg 40, then we have more dimensions to deal with, then the most frequently appearing column are kept and less frequent columns are taken out as a other column which is a single column consisting of less frewuent columns
