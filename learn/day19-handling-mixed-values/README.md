### handling Mixed values
When two types of information is within a single variable that will be considered as mixed vales
> It has to be splitted
Or in a column there are mix numerical and categorical values
> Then it has to be slitted into two columns
